<%- include("includes/header.ejs") %>
    <link rel="stylesheet" href="/style.css">
    <title>Склад <%= stock.name %></title>
</head>
<body>
    <%- include("includes/navbar.ejs", { page: "car" }) %>
    <% if (error) { %>
        <div class="err-card">
            <h1><%=error%></h1>
        </div>
    <% } else { %>
        <div class="simple-card">
            <h1>Найден склад <span class="inv"><%=stock.name%></span>!</h1>

            <% if (stock.cars.length) { %>
                <h1>Машины:</h1>

                <% for (const car of stock.cars) { %>
                    <div class="simple-card">
                        <p class="bp">Название: <%=car%></p>
                    </div>
                <% } %>

            <% } else { %>
                <div class="err-card">
                    <h1>Машин на складе не найдено</h1>
                </div>
            <% } %>
        </div>
    <% } %>

<%- include("includes/end.ejs") %>
